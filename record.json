{
  "description": "Response format for a single object / record",
  "extends": "./base.json",
  "properties": {
    "result": {
      "description": "The response record",
      "type": "object",
      "required": true
    }
  }
}
